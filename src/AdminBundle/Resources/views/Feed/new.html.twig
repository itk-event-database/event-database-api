{% extends 'AdminBundle:Feed:_edit.html.twig' %}

{% block body -%}
  <h1>{{ 'Create feed' | trans }}</h1>

  {{ form_start(form) }}
  {{ form_rest(form) }}
  <div class="actions">
    <a class="btn btn-default" href="{{ path('admin_feed') }}">{{ 'Cancel' | trans  }}</a>
    <button class="btn btn-primary" type="submit">{{ 'Create' | trans }}</button>
  </div>
  {{ form_end(form) }}

  <div id="jsoneditor"></div>
{% endblock %}
