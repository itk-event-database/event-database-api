{% extends 'AdminBundle::base.html.twig' %}

{% block body -%}
  <h1>{{ 'Create unknown tag' | trans }}</h1>

  {{ form_start(form) }}
  {{ form_rest(form) }}
	<div class="actions">
		<a class="btn btn-default" href="{{ path('admin_unknown_tag') }}">{{ 'Cancel' | trans  }}</a>
		<button class="btn btn-primary" type="submit">{{ 'Create' | trans }}</button>
	</div>
  {{ form_end(form) }}
{% endblock %}
